# Widgets Layer

Widgets compose multiple features/entities into page-level sections.

Examples:

- dashboard metric board
- support ticket board
- bookings analytics panel

Widgets should not contain direct API calls; delegate to features/entities.
